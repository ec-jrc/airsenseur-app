// angular.module('senseEurAir.filters', [])

// .filter('inRadius', function () {
//   return function (items) {
//     var filtered = [];
//     for (var i = 0; i < items.length; i++) {
//       var item = items[i];
//       if (/a/i.test(item.name.substring(0, 1))) {
//         filtered.push(item);
//       }
//     }
//     return filtered;
//   };
// });

// app.filter('inRadius', function () {
//   return function (items, radius) {
//     var filtered = [];
//     var letterMatch = new RegExp(letter, 'i');
//     for (var i = 0; i < items.length; i++) {
//       var item = items[i];
//       if 
//       if (letterMatch.test(item.name.substring(0, 1))) {
//         filtered.push(item);
//       }
//     }
//     return filtered;
//   };
// });